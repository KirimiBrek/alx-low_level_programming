#include "main.h"
#include <unistd.h>

/**
 * _puts - prints a string to stdout
 * @str: pointer to the string to print
 */
void _puts(char *str)

/**
 * main-checks the code for string
 *
 */
int main(void)
{
	char *str;

	str = "Holberton!";
	_puts(str);
	return (0);
}
